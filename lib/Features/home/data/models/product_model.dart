class ProductModel {
  final String name, image, price, rate, rateCount;

  ProductModel({
    required this.name,
    required this.image,
    required this.price,
    required this.rate,
    required this.rateCount,
  });
}
